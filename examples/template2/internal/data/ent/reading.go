// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DrReMain/cyber-ecosystem/examples/template2/internal/data/ent/reading"
)

// Reading is the model entity for the Reading schema.
type Reading struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 文章ID
	BlogID string `json:"blog_id,omitempty"`
	// 阅读量
	ReadingCount int64 `json:"reading_count,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Reading) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case reading.FieldReadingCount:
			values[i] = new(sql.NullInt64)
		case reading.FieldID, reading.FieldBlogID:
			values[i] = new(sql.NullString)
		case reading.FieldCreatedAt, reading.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Reading fields.
func (_m *Reading) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case reading.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case reading.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case reading.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case reading.FieldBlogID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field blog_id", values[i])
			} else if value.Valid {
				_m.BlogID = value.String
			}
		case reading.FieldReadingCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field reading_count", values[i])
			} else if value.Valid {
				_m.ReadingCount = value.Int64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Reading.
// This includes values selected through modifiers, order, etc.
func (_m *Reading) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Reading.
// Note that you need to call Reading.Unwrap() before calling this method if this Reading
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Reading) Update() *ReadingUpdateOne {
	return NewReadingClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Reading entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Reading) Unwrap() *Reading {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Reading is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Reading) String() string {
	var builder strings.Builder
	builder.WriteString("Reading(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("blog_id=")
	builder.WriteString(_m.BlogID)
	builder.WriteString(", ")
	builder.WriteString("reading_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.ReadingCount))
	builder.WriteByte(')')
	return builder.String()
}

// Readings is a parsable slice of Reading.
type Readings []*Reading
