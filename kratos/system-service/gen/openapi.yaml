# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: UserService API
    description: |-
        UserService 系统用户服务
         提供用户的创建、更新、删除、查询等核心操作接口
    version: 0.0.1
paths:
    /api/v1/system/user:
        post:
            tags:
                - UserService
            description: |-
                CreateUser 创建用户接口
                 用于新增系统用户，包含用户名、邮箱、年龄、密码等信息的校验
                 @summary 创建系统用户
                 @description 新增系统用户，需满足用户名、邮箱、年龄、密码等校验规则，密码需二次确认
                 @tags 系统用户管理
                 @accept json
                 @produce json
                 @param body body CreateUserRequest true "创建用户请求参数"
                 @success 200 {object} CreateUserResponse "创建成功响应"
            operationId: UserService_CreateUser
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/api.v1.CreateUserRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.v1.CreateUserResponse'
components:
    schemas:
        api.v1.CreateUserRequest:
            type: object
            properties:
                username:
                    type: string
                    description: 用户名：不能包含"admin"关键词
                email:
                    type: string
                    description: 用户邮箱：必须以@cyber.com结尾（公司内网邮箱）
                age:
                    type: integer
                    description: 用户年龄：18 ≤ 年龄 < 150
                    format: int32
                password:
                    type: string
                    description: 用户密码：长度至少8位
                confirm_password:
                    type: string
                    description: 确认密码：需与密码字段一致
            description: |-
                CreateUserRequest 创建用户请求参数
                 包含创建用户所需的所有字段及校验规则
        api.v1.CreateUserResponse:
            type: object
            properties: {}
            description: |-
                CreateUserResponse 创建用户响应结果
                 创建用户接口的返回数据结构
tags:
    - name: UserService
