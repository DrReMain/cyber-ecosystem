{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the Kratos base layout",
      "x-prompt": "What is the name of your Kratos service?",
      "pattern": "^[a-z][a-z0-9-]*$",
      "x-prompt-error-message": "Name must start with a lowercase letter and contain only lowercase letters, numbers, and hyphens"
    },
    "directory": {
      "type": "string",
      "description": "The directory where the project will be generated",
      "x-prompt": "Which directory should the project be placed in?",
      "default": "services"
    },
    "tags": {
      "type": "string",
      "description": "Comma-separated list of tags for the project (optional)",
      "default": ""
    }
  },
  "required": ["name"],
  "additionalProperties": false
}
