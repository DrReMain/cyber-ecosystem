apiVersion: batch/v1
kind: Job
metadata:
  name: glitchtip-setup
  namespace: observability
spec:
  template:
    spec:
      containers:
        - name: setup
          env:
            - name: GLITCHTIP_DOMAIN
              value: http://errors.example.com
