{
  "$schema": "../../../../.nx/installation/node_modules/nx/schemas/project-schema.json",
  "name": "genesis_service_admin_bff",
  "root": "apps/genesis/services/admin_bff",
  "projectType": "application",
  "implicitDependencies": ["genesis_api", "genesis_service_base", "shared-go"],
  "targets": {
    "proto:conf": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/apps",
        "command": "buf generate --template genesis/services/admin_bff/buf.gen.conf.yaml --path genesis/services/admin_bff/internal/conf"
      }
    },
    "generate:i18n": {
      "executor": "nx:run-commands",
      "dependsOn": ["genesis_api:proto:api"],
      "options": {
        "cwd": "{projectRoot}",
        "command": "go generate ./internal/i18n/..."
      }
    },
    "generate:wire": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "go run -mod=mod github.com/google/wire/cmd/wire ./cmd/app"
      }
    },
    "generate": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "genesis_api:proto:api",
        "proto:conf",
        "generate:i18n",
        "generate:wire"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["go mod tidy"],
        "parallel": false
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "kratos run"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/bin"],
      "options": {
        "cwd": "{projectRoot}",
        "command": "go build -ldflags \"-X main.Version=$(git describe --tags --always)\" -o ./bin/ ./..."
      }
    }
  }
}
