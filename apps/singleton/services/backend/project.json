{
  "$schema": "../../../../.nx/installation/node_modules/nx/schemas/project-schema.json",
  "name": "singleton_backend",
  "root": "apps/singleton/services/backend",
  "projectType": "application",
  "implicitDependencies": ["singleton_api", "shared-go"],
  "targets": {
    "proto:conf": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}/apps",
        "command": "buf generate --template singleton/services/backend/buf.gen.conf.yaml --path singleton/services/backend/internal/conf"
      }
    },
    "generate:i18n": {
      "executor": "nx:run-commands",
      "dependsOn": ["singleton_api:proto:api"],
      "options": {
        "cwd": "{projectRoot}",
        "command": "go generate ./internal/server/..."
      }
    },
    "generate": {
      "executor": "nx:run-commands",
      "dependsOn": ["singleton_api:proto:api", "proto:conf"],
      "options": {
        "cwd": "{projectRoot}",
        "commands": [
          "go generate ./...",
          "go mod tidy"
        ],
        "parallel": false
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "kratos run"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/bin"
      ],
      "options": {
        "cwd": "{projectRoot}",
        "command": "go build -ldflags \"-X main.Version=$(git describe --tags --always)\" -o ./bin/ ./..."
      }
    },
    "ent:new": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "go run -mod=mod entgo.io/ent/cmd/ent new --target=internal/data/ent/schema {args.Entity}"
      }
    }
  }
}
