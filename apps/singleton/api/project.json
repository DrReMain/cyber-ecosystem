{
  "$schema": "../../../.nx/installation/node_modules/nx/schemas/project-schema.json",
  "name": "singleton_api",
  "root": "apps/singleton/api",
  "projectType": "application",
  "implicitDependencies": ["contracts"],
  "targets": {
    "proto:api": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "buf generate --template apps/singleton/api/buf.gen.api.yaml --path apps/singleton/api"
      }
    }
  }
}
